{
  "_comment": "Prompt: use the DeepSeek tool to fix issues for this chat session. Reason: run the Electron main process directly so F5 attaches breakpoints to its browser and IPC code.",
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Techkes Electron",
      "type": "node",
      "request": "launch",
      "cwd": "${workspaceFolder}",
      "runtimeExecutable": "${workspaceFolder}/node_modules/electron/dist/electron.exe",
      "args": [
        "${workspaceFolder}"
      ],
      "console": "integratedTerminal",
      "skipFiles": [
        "<node_internals>/**"
      ]
    }
  ]
}
